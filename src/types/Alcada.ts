export type Alcada = {
    id: number,
    centro_custo: string,
    centro_custo_nome: string,
    // Campos extras usados no mapeamento da conta contábil.
    conta_contabel?: string,
    conta_contabel_nome?: string
}